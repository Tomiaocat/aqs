server:
  port: 8080

spring:
  application:
    name: http-encryption-server

# 加密配置
api:
  encrypt:
    key: your-secret-key-here  # 与服务端约定的密钥
    enabled: true              # 是否启用加密

# 日志配置
logging:
  level:
    com.httpserver: DEBUG
